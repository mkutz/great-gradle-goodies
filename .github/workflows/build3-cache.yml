name: Build Cache

on:
  push:
    branches:
      - main
    paths:
      - services/cache/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          overwrite-settings: false

      - uses: gradle/actions/setup-gradle@v3

      - run: ./gradlew :services:cache:check --no-daemon
